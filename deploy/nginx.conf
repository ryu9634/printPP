# Nginx 설정 파일
# 위치: /etc/nginx/sites-available/portfolio

upstream portfolio_backend {
    server localhost:8080;
    keepalive 32;
}

server {
    listen 80;
    server_name YOUR_DOMAIN.com www.YOUR_DOMAIN.com;  # 실제 도메인으로 변경

    # 최대 업로드 크기
    client_max_body_size 10M;

    # 로그 설정
    access_log /var/log/nginx/portfolio_access.log;
    error_log /var/log/nginx/portfolio_error.log;

    # 정적 파일 (uploads) 직접 서빙
    location /uploads/ {
        alias /opt/portfolio/uploads/;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    # Spring Boot 애플리케이션으로 프록시
    location / {
        proxy_pass http://portfolio_backend;

        # 프록시 헤더 설정
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # 타임아웃 설정
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;

        # 버퍼 설정
        proxy_buffering on;
        proxy_buffer_size 4k;
        proxy_buffers 8 4k;

        # Keep-Alive 설정
        proxy_http_version 1.1;
        proxy_set_header Connection "";
    }

    # 에러 페이지 (선택 사항)
    error_page 500 502 503 504 /50x.html;
    location = /50x.html {
        root /usr/share/nginx/html;
    }
}

# HTTPS 설정 (Let's Encrypt SSL 인증서 사용 시)
# Certbot으로 SSL 인증서를 발급받은 후 자동으로 추가됩니다.
#
# server {
#     listen 443 ssl http2;
#     server_name YOUR_DOMAIN.com www.YOUR_DOMAIN.com;
#
#     ssl_certificate /etc/letsencrypt/live/YOUR_DOMAIN.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/YOUR_DOMAIN.com/privkey.pem;
#     include /etc/letsencrypt/options-ssl-nginx.conf;
#     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
#
#     # 위의 location 설정들을 여기에도 동일하게 복사
# }
#
# # HTTP to HTTPS 리다이렉트
# server {
#     listen 80;
#     server_name YOUR_DOMAIN.com www.YOUR_DOMAIN.com;
#     return 301 https://$server_name$request_uri;
# }
